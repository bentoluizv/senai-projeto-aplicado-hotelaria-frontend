---
type Props = {
  id: string;
  label: string;
  value?: string;
  disabled?: boolean;
  hidden?: boolean;
};

const { id, label, value, disabled, hidden } = Astro.props;
---

<div class="flex flex-col p-2 w-full">
  <label hidden={hidden} for={id}>{label}</label>
  <input
    class="appearance-none p-2 border border-zinc-300 rounded"
    type="text"
    id={id}
    name={id}
    required
    value={value}
    disabled={disabled}
    hidden={hidden}
  />
</div>
